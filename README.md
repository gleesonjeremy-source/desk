# desk

The Desk — human window on the trading machine. Static page; data lives behind a locked Firebase path (key never in this repo). Source of truth: desk.html in the private options-scanner repo — edit there, copy here, push.
